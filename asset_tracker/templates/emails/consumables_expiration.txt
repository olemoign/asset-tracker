{{ gettext('Dear user') }},

{{ gettext('The following consumables expire in %(delay_days)s days on %(expiration_date)s:', delay_days=delay_days, expiration_date=expiration_date|format_date(locale, 'long')) }}

{% for consumable in expired_consumables %}
    * {{ gettext(consumable) }}
{% endfor %}

{{ gettext('Please know that there is no guarantee that diagnoses will be reliable if the consumable expiration dates have passed.') }}

{{ gettext('To view the asset, follow') }} {{ gettext('this link') }} : {{ 'assets-update'|route_url(asset_id=asset.asset_id) }}.

{{ gettext('Sincerely') }},
{{ gettext(cloud_name) }}
