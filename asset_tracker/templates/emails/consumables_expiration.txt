{{ gettext('Dear user') }},

{{ gettext('The equipment') }} {{ gettext(model) }} {{ gettext('of your asset %(asset_id)s has the following consumables which will expire in %(delay_days)s days on %(expiration_date)s:',
                asset_id=asset_id, delay_days=delay_days, expiration_date=expiration_date|format_date(locale, 'long')) }}

{% for consumable_label in consumable_labels %}
- {{ gettext(consumable_label) }}
{% endfor %}

{{ gettext('Please know that there is no guarantee that diagnoses will be reliable if the consumable expiration dates have passed.') }}

{{ gettext('To view the asset, follow ') }} {{ gettext('this link') }}: <a href="{{ asset_url }}">.

{{ gettext('Sincerely') }},
{{ gettext(app_name) }}
