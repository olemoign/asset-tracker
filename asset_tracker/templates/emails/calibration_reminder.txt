{{ gettext('Dear user') }},

{{ gettext('The following assets must be calibrated before %(calibration_date)s',
           app_name=app_name, calibration_date=calibration_date|format_date(locale, 'long')) }}:

{% for asset in assets %}
    * {{ asset.asset_id }} : {{ assets_url }}/{{ asset.id }}/
{% endfor %}

{{ gettext('Sincerely') }},
{{ gettext(app_name) }}
